[package]
name = "cifar10"
version = "0.1.0"
edition = "2024"

[dependencies]
burn = { version = "*", features = ["train", "candle"]}
image = "0.25.6"
serde = { version = "1.0.219", features = ["derive"] }
